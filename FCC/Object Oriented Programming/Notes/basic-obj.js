/**
 * Create a dog object with name and numLegs property, set them to
 * a string and number, respectively.
 * And access the properties using dot notation
 */

let dog = {
    name: "senior",
    numLegs: 4,
};

const str = `The of the dog is ${dog.name} and has ${dog.numLegs} legs`;
console.log(str);
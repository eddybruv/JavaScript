/**
 * A greedy match finds the longest possible part of a 
 * string that fits the regex pattern and returns it as a
 * match.
 * 
 * eg
 * /t[a-z]*i/ to the string "titanic" would return ["titani"]
 */
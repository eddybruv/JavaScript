/**
 * @brief basic arrays
 */

var numbers = [4, 6, 3, 2, 4, 3, 6, 4, 2, 2, 5, 6, 3];

//nested arrays(arrays of arrays)
var arr = [
    [4, 3, 4],
    [34, 2, 4],
    [2, 4],
    [41, 3, 2]
];

//we can also use the keyword "new"
var cars = new Array("toyota", "camry", "hummer");